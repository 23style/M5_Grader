# M5Grader Config Editor 機能仕様書

## 1. 概要

### 1.1 ツール名
M5Grader Config Editor

### 1.2 目的
M5GraderシステムのSDカード用 `config.json` をPCで視覚的に編集するGUIツール。
規格（grades）ごとの**背景色**をパレットで確認・選択しながら編集できる。

### 1.3 環境
- 言語: Python 3.x
- GUIライブラリ: tkinter（Python標準ライブラリ、追加インストール不要）
- 動作OS: Windows 10/11

---

## 2. 機能一覧

| ID  | 機能名               | 説明 |
|-----|----------------------|------|
| F01 | ファイルを開く       | ダイアログでJSONファイルを選択して読み込む |
| F02 | 上書き保存           | 現在のファイルパスに保存 |
| F03 | 名前を付けて保存     | 別名でJSONファイルを保存 |
| F04 | ネットワーク設定編集 | WiFi SSID/パスワード、GAS URLを編集 |
| F05 | デバイス設定編集     | デバイスID、計量記録 有効/無効 を編集 |
| F06 | 製品設定編集         | 製品名、重量範囲、安定閾値を編集 |
| F07 | 規格一覧表示         | grades を表形式で表示。色列はその規格の背景色で塗りつぶして表示 |
| F08 | 規格追加             | 新しい規格行をダイアログで追加 |
| F09 | 規格編集             | 選択行をダイアログで編集（行ダブルクリックも可） |
| F10 | 規格削除             | 選択行を削除（確認ダイアログ付き） |
| F11 | 規格並び替え         | 選択行を上/下に移動 |
| F12 | カラーパレット選択   | ダイアログ内に固定パレットを表示し、クリックで色を選択。HEXコード直接入力も可 |
| F13 | 規格範囲チェック     | 規格間の重複・ギャップを検出し、テーブル下部にリアルタイム表示 |
| F14 | 入力バリデーション   | 保存時に必須項目・数値範囲・カラーコード形式を検証 |
| F15 | 未保存変更警告       | ウィンドウを閉じる/別ファイルを開く時に確認 |

---

## 3. 画面仕様

### 3.1 メインウィンドウ

- タイトル: `M5Grader Config Editor`
- 初期サイズ: 800 × 700 px（リサイズ可）
- レイアウト: 上から下へ縦並び、スクロール可能な1ページ構成

#### 3.1.1 ファイルバー（最上部）

| 要素 | 説明 |
|------|------|
| [開く] ボタン | ファイル選択ダイアログ（*.json）を表示 |
| ファイルパス表示欄 | 現在開いているファイルのフルパス（読み取り専用） |
| [保存] ボタン | 上書き保存（未選択時はダイアログ表示） |
| [名前を付けて保存] ボタン | 保存ダイアログを表示 |

#### 3.1.2 ネットワーク設定セクション

| フィールド | 型 | JSONキー | 備考 |
|------------|-----|----------|------|
| WiFi SSID | テキスト | `wifi.ssid` | 必須 |
| WiFi パスワード | パスワード＋[表示]切替ボタン | `wifi.password` | 必須 |
| GAS URL | テキスト（長い） | `gas_url` | 必須 |

#### 3.1.3 デバイス設定セクション

| フィールド | 型 | JSONキー | 備考 |
|------------|-----|----------|------|
| デバイス ID | 整数 | `device_id` | 必須、1以上 |
| 計量記録 | チェックボックス | `recording_enabled` | 任意（省略時は true 扱い。保存時は書き出す） |

#### 3.1.4 製品設定セクション

| フィールド | 型 | JSONキー |
|------------|-----|----------|
| 製品名 | テキスト | `product_settings.product_name` |
| 最小測定重量 | 小数（g） | `product_settings.min_weight` |
| 最大測定重量 | 小数（g） | `product_settings.max_weight` |
| 安定判定閾値 | 小数（g） | `product_settings.stability_threshold` |

#### 3.1.5 規格設定セクション（grades テーブル）

**ツールバーボタン**（テーブル上部）

| ボタン | 動作 |
|--------|------|
| [＋ 追加] | 規格編集ダイアログを新規モードで開く |
| [✏ 編集] | 選択行を編集モードで開く |
| [🗑 削除] | 選択行を削除（確認ダイアログ） |
| [↑ 上へ] | 選択行を1つ上に移動 |
| [↓ 下へ] | 選択行を1つ下に移動 |

**テーブル列**

| 列名 | 内容 | 幅 |
|------|------|----|
| No. | 順番（1始まり） | 42px |
| 規格名 | name | 70px |
| 最小重量 (g) | min_weight | 95px |
| 最大重量 (g) | max_weight | 95px |
| 音声ファイル | sound_file | 伸縮 |
| 背景色 | **その規格の色（#RRGGBB）で塗りつぶした色見本セル**。HEXコードを白または黒のテキストで表示 | 130px |

**背景色列の表示仕様**
- セルの背景 = 規格の `color` 値で塗りつぶし
- テキスト（HEXコード）の色 = 明るい背景には黒、暗い背景には白（輝度で自動判定）
- 輝度計算式: `Y = 0.299×R + 0.587×G + 0.114×B`。Y < 128 なら白、Y ≥ 128 なら黒

**インタラクション**
- 行をダブルクリック → 編集ダイアログを開く
- 行選択なし → 編集・削除・↑↓ ボタンを無効化
- 先頭行選択 → ↑ ボタンを無効化
- 末尾行選択 → ↓ ボタンを無効化

**範囲チェック表示**（テーブル直下）

規格を追加・編集・削除・並び替えするたびにリアルタイム更新。

| 状態 | 表示例 |
|------|--------|
| 問題なし | ✅ 範囲チェック: OK |
| 重複あり | ⚠ 重複: 5L (320–349) と 4L (290–319) が重なっています |
| ギャップあり | ⚠ ギャップ: M (160–189) と S (130–159) の間が空いています |

チェック内容:
1. 各規格内: min_weight < max_weight であること
2. 規格間（min_weight でソート後に隣接比較）:
   - 重複: `grades[i].max_weight >= grades[i+1].min_weight`
   - ギャップ: `grades[i].max_weight + 1 < grades[i+1].min_weight`

#### 3.1.6 ステータスバー（最下部）

| 要素 | 説明 |
|------|------|
| 保存状態 | `保存済み` / `● 未保存の変更あり`（赤色） |
| 規格数 | `規格数: N` |

---

### 3.2 規格編集ダイアログ

- タイトル: `規格を追加` / `規格を編集`
- サイズ: 460 × 420 px（固定）

**入力フィールド**

| フィールド | 型 | 補足 |
|------------|-----|------|
| 規格名 | テキスト | 必須 |
| 最小重量 | 整数（g） | 必須、0以上 |
| 最大重量 | 整数（g） | 必須、最小重量より大きい |
| 音声ファイル | テキスト ＋ [参照...] ボタン | 必須。[参照...] で *.wav を選択 |

**背景色選択エリア**

- ダイアログ内に常時パレットを表示（ポップアップしない）
- パレット構成: 4行 × 8列 = 32色のカラースウォッチ
  - 行1: 赤〜黄緑系
  - 行2: 緑〜青紫系
  - 行3: ピンク〜暗色系
  - 行4: 白〜黒のグレースケール
- パレット下: HEXコード入力欄（`#RRGGBB` 形式）＋ 色プレビュー（横長の色見本）
- クリックしたスウォッチ → HEX入力欄に反映、プレビュー更新、選択枠表示
- HEX入力欄を変更してフォーカスアウト → プレビュー更新

**ボタン**
- [OK] → バリデーション後に閉じてテーブルに反映
- [キャンセル] → 変更を破棄して閉じる

---

## 4. バリデーションルール

| 対象 | ルール |
|------|--------|
| WiFi SSID | 1文字以上 |
| GAS URL | `https://` で始まる |
| デバイスID | 1以上の整数 |
| 製品名 | 1文字以上 |
| 最小測定重量 | 0以上の数値 |
| 最大測定重量 | 最小測定重量より大きい |
| 安定閾値 | 0より大きい数値 |
| 規格名 | 1文字以上 |
| 規格最小重量 | 0以上の整数 |
| 規格最大重量 | 規格最小重量より大きい |
| 背景色 | `#RRGGBB` 形式（正規表現 `^#[0-9A-Fa-f]{6}$`） |

---

## 5. ファイル操作詳細

### 5.1 読み込み
- JSON解析エラー → エラーダイアログ表示
- 未知フィールドは保持（削除しない）
- `recording_enabled` は存在しない場合もチェックボックスを表示（デフォルト: チェックなし）

### 5.2 書き込み
- インデント: 2スペース
- 日本語等はASCIIエスケープしない（`ensure_ascii=False`）
- 末尾改行あり
- 保存成功時: ステータスバーに「保存済み」を表示

### 5.3 未保存変更の検出
- 任意のフィールド変更時にステータスバーを「● 未保存の変更あり」に更新
- ウィンドウを閉じる時・別ファイルを開く時に確認ダイアログ

---

## 6. カラーパレット 色リスト

| 行 | 色（左→右） |
|----|-------------|
| 1 | #FF0000, #FF4500, #FF8C00, #FFA500, #FFD700, #FFFF00, #ADFF2F, #00FF00 |
| 2 | #00FF7F, #00FFFF, #00BFFF, #0080FF, #0000FF, #4B0082, #8B00FF, #FF00FF |
| 3 | #FF69B4, #FF1493, #DC143C, #800000, #808000, #006400, #008080, #000080 |
| 4 | #FFFFFF, #D0D0D0, #A0A0A0, #808080, #606060, #404040, #202020, #000000 |

---

## 7. 使用ライブラリ

| ライブラリ | 用途 |
|------------|------|
| `tkinter` | メインGUI |
| `tkinter.ttk` | テーブル（Treeview）、スタイル |
| `tkinter.filedialog` | ファイル選択・保存ダイアログ |
| `tkinter.messagebox` | 確認・エラー・情報ダイアログ |
| `json` | JSON読み書き |
| `re` | カラーコード正規表現検証 |
| `pathlib` | ファイルパス操作 |

すべて Python 標準ライブラリのため、追加インストール不要。
