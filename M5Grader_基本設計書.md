# M5Grader システム基本設計書

## 1. システム概要

### 1.1 システム名
M5Grader - 汎用重量分類システム

### 1.2 システム目的
M5Stack Coreを使用した、重量に基づく製品の自動分類・音声案内システム。設定ファイルによる柔軟な分類基準変更により、様々な製品（果物、野菜等）の選果作業に対応可能。

### 1.3 対象ハードウェア
- M5Stack Core
- TAL221ロードセル（重量センサー）
- M5UnitWeightI2C（重量測定ユニット）
- SDカード
- 内蔵スピーカー

## 2. 要件定義

### 2.1 機能要件

#### 2.1.1 重量測定機能
- 連続的な重量測定
- 測定値の安定性判定
- オフセット調整機能
- キャリブレーション機能

#### 2.1.2 製品分類機能
- 重量に基づく規格判定
- 動的な分類基準設定
- 複数規格への対応

#### 2.1.3 音声案内機能
- 規格別音声再生
- システム状態音声通知
- WAVファイル形式対応

#### 2.1.4 表示機能
- 規格名の画面表示
- 背景色による視覚的識別
- 重量値表示
- 測定状態表示
- デバイスID表示（画面右上に常時表示）

#### 2.1.5 設定管理機能
- SDカードベース設定ファイル
- 製品別設定の動的読み込み
- エラー時詳細表示

#### 2.1.6 ネットワーク機能
- **デフォルトはオフライン**（起動時にWiFi接続しない）
- 計量結果は都度SDカード上のCSVログファイルに追記
- ボタンC（Upload）押下時のみ WiFi接続 → 全ログをGASにHTTP POST → WiFi切断
- Google Apps Script連携（HTTP POST）
- HTTP リダイレクト処理（302エラー対応）
- アップロード失敗行のみを再送用に保持（次回アップロード時に自動再試行）

### 2.2 非機能要件

#### 2.2.1 性能要件
- 測定間隔：100ms
- 安定判定：4サンプル履歴
- 応答時間：0.4秒以内（安定判定確定まで）
- 単発読取：5サンプル取得後、最小・最大を除いた中央3サンプルのトリム平均（サンプル間隔5ms、合計約25ms）

#### 2.2.2 信頼性要件
- 設定エラー時の適切な中断処理
- 適応的ゼロトラッキングによる連続ドリフト補正
- ソフトウェアゼロ補正 + ハードウェアオフセットの二段構え
- 音声ファイル不存在時の継続動作
- WiFi接続失敗時の自動オフライン切替
- ネットワーク通信エラー時の継続動作
- HTTPリダイレクト自動処理

#### 2.2.3 操作性要件
- ボタン操作による基本機能実行
- 視覚的フィードバック（色分け表示）
- 音声による直感的な操作案内

## 3. システム仕様

### 3.1 ハードウェア仕様

#### 3.1.1 重量センサー
- センサー：TAL221ロードセル
- 測定範囲：設定可能（config.jsonで指定）
- 精度：1g単位
- インターフェース：I2C通信

#### 3.1.2 表示装置
- ディスプレイ：M5Stack Core内蔵LCD
- 解像度：320×240ピクセル
- 表示内容：規格名、重量値、状態表示、デバイスID
- 画面レイアウト：
  - 中央：規格名（大きく表示、背景色付き）
  - 下部：重量値（190px位置）
  - 右下：測定状態（230px位置）
  - 右上：デバイスID（白背景四角囲み、最上位レイヤー）

#### 3.1.3 音声出力
- スピーカー：M5Stack Core内蔵
- ファイル形式：WAV
- 音量：設定可能

### 3.2 ソフトウェア仕様

#### 3.2.1 設定ファイル仕様（config.json）
```json
{
  "wifi": {
    "ssid": "WiFiネットワーク名",
    "password": "WiFiパスワード"
  },
  "gas_url": "Google Apps ScriptのURL",
  "device_id": "デバイス識別番号",
  "recording_enabled": true,
  "product_settings": {
    "product_name": "製品名",
    "min_weight": "最小測定重量(g)",
    "max_weight": "最大測定重量(g)",
    "stability_threshold": "安定判定閾値(g)",
    "grades": [
      {
        "name": "規格名",
        "min_weight": "最小重量(g)",
        "max_weight": "最大重量(g)",
        "sound_file": "音声ファイルパス",
        "color": "16進カラーコード"
      }
    ]
  }
}
```

#### 3.2.2 測定状態定義
- **STATE_MEASURING**: 測定中（値が変動中）
- **STATE_STABLE**: 測定値安定（規格確定）
- **STATE_ZERO**: ゼロ状態（物体なし）

#### 3.2.3 ボタン機能（通常時）
- **ボタンA**: オフセット設定（確認ダイアログ経由）
- **ボタンB**: 無反応
- **ボタンC**: メニュー画面表示
- 画面下部ボタンガイド：**Offset / (空) / Menu**

#### 3.2.4 メニュー画面
- ボタンC押下で表示
- メニュー項目：Upload / Record: ON|OFF / Calibrate / Back
- 操作：**A=Back、B=Down、C=Select**
- 10秒無操作で自動的にBack
- Record項目は現在の状態を表示、選択で ON/OFF トグル（config.jsonに保存）
- Upload・Calibrate選択時は各々の確認ダイアログ経由で実行

#### 3.2.4 確認ダイアログ共通仕様
- 全ての破壊的操作（Offset、Calibrate、WiFi切替）は確認画面を経由
- 10秒無操作でNo扱い（自動キャンセル）
- デフォルトはNo（No側のボタンを白背景で強調表示）
- ダイアログ終了時に「全ボタン離しを待機」＋「未処理ボタンイベントを消費」
  → メインループでのボタン誤動作（例：C押下キャンセル後にWiFi切替が誤発火する不具合）を防止
- ボタン割り当て（全ダイアログ統一）：**A=Yes（実行） / B・C=No（キャンセル）**
- キャリブレーションYes選択時、3→2→1のカウントダウン後に実行
- 起動時の初期オフセットは確認なしで自動実行

### 3.3 ファイル構成
- **SDカードルート**:
  - `config.json` - システム設定ファイル（`recording_enabled` フィールドも記録）
  - `*.wav` - 規格別音声ファイル
  - `info.wav` - システム情報音
  - `error.wav` - エラー音
  - `measurements.csv` - 現在の計量結果ログ（自動生成）
  - `measurements_arc_N.csv` - ローテーション済みアーカイブ（N=0,1,2...）
  - `measurements_pending_<millis>.csv` - アップロード開始時に隔離した一時ファイル
  - `measurements_retry.csv` - アップロード失敗行の再送用
- **CSVフォーマット**: `timestamp,size,weight,device_id`（1計量=1行）
- **アップロード対象**：`measurements.csv` 以外の `measurements_*.csv` すべて

#### 3.3.1 レコード数管理
- **カウント方法**：起動時に全ペンディングファイルの行数を集計、以降 appendToLog で加算、アップロード後に再集計
- **警告表示**：Menu ラベルの色で通知
  - < 8000件：白
  - 8000〜9999件：黄色（Uploadを促す）
  - >= 10000件：赤（即Upload推奨）＋自動ローテーション実行
- **ローテーション**：`measurements.csv` を `measurements_arc_N.csv` にリネームし新規ファイル開始

## 4. 基本動作フロー

### 4.1 システム起動
1. M5Stack Core初期化
2. SDカード初期化確認
3. config.json読み込み・検証
4. 重量センサー初期化
5. キャリブレーション係数読み込み
6. 初期オフセット設定
（WiFi接続はしない。Uploadボタン押下時に接続する）

### 4.2 通常測定フロー
1. 重量値取得（5サンプル取得→min/max除去→中央3サンプル平均、サンプル間隔5ms）
2. ソフトウェアゼロ補正の適用
3. 安定性判定（4サンプル履歴、100ms間隔）
4. 測定状態更新
5. 規格判定（重量範囲マッチング）
6. 画面表示更新（規格名・背景色）
7. 音声再生（規格確定時）
8. SDカードのCSVログファイルへ追記（規格確定時、1計量=1行）

### 4.3 エラーハンドリング
1. SDカード初期化失敗 → 赤画面表示・処理中断
2. config.json不存在 → 赤画面表示・処理中断
3. JSON解析エラー → 赤画面表示・処理中断
4. 必須設定項目不足 → 赤画面表示・処理中断
5. 重量センサーエラー → エラー音・再試行
6. アップロード時 WiFi接続失敗 → 赤画面表示・ログ保持・通常測定へ復帰
7. アップロード時 GAS通信エラー → 失敗行のみログに残し次回再送、完了画面に失敗件数表示

### 4.3.1 アップロードフロー（メニュー Upload 選択時）
1. 確認ダイアログ表示（A=Yes / B・C=No、10秒無操作でキャンセル）
2. 実行中は計量ブロック（同期処理のためメインループが停止）
3. `/measurements.csv` を `/measurements_pending_<millis>.csv` に隔離（新規計量は新ファイルへ）
4. `measurements_*.csv` 全て（現在ファイルを除く）をアップロード対象に列挙
5. WiFi 接続（失敗時はエラー表示してログ保持し中断）
6. NTP 時刻同期（以降のRTCも実時刻で動作）
7. 各ファイルを1行ずつ読み、**BATCH_SIZE（50件）ごとに1回のPOST**でまとめて送信
   - JSONペイロード形式：`{ "device_id": N, "records": [{"timestamp":..,"size":..,"weight":..}, ...] }`
   - GAS側は `records` 配列を受けてスプレッドシートに複数行追加する実装が必要
   - バッチ単位で成功/失敗を判定。成功バッチ内のレコードは削除、失敗バッチは再送リストへ
8. 処理済みファイルを削除
9. 失敗行を `measurements_retry.csv` に保存（次回アップロード時に再送）
10. WiFi 切断、レコード数を再集計
11. 完了画面表示（成功/失敗件数）
12. 測定状態リセット、通常測定へ復帰

### 4.3.2 バッチPOST採用理由
- 1レコード1POST方式では 10,000件で ~14時間 かかる
- バッチ50件/POST で 10,000件を ~7分に短縮
- 応答時間短縮により、レコード溜め置き運用が実用的になる

### 4.3.3 タイムスタンプ
- 起動直後～初回アップロード前：NTP未同期のため `Boot+Nms`（ブート後経過ミリ秒）形式
- 初回アップロード後：NTP同期完了、以降は `YYYY/MM/DD HH:MM:SS` 形式
- 電源OFFでRTCはリセット。次回起動時は再び `Boot+Nms` から

### 4.4 自動機能
- **適応的ゼロトラッキング（ドリフト補正）**:
  - ソフトウェアゼロオフセットを保持し、表示値から常時減算
  - 測定値が ZERO_THRESHOLD 以下で安定している時、その値を指数移動平均でソフトウェアゼロに反映
  - ソフトウェアゼロが ±3g を超えた場合、または 30秒以上ゼロ付近で安定した場合、ハードウェア setOffset() を実行しソフトウェアゼロをリセット
  - 5分間隔の制限は廃止し、条件ベースで常時補正
- **自動ログ追記**: 規格確定時にSDカードのCSVログファイルへ1行追記
- **安定性検出**: 連続4サンプルが閾値内で安定判定
- **STATE_STABLEラッチ**: 一度安定確定した後、STATE_ZERO（物体除去）まで状態・表示・音声を保持（サイズ連呼防止）
- **トリム平均によるノイズ除去**: 単発測定でmin/maxを除外し、外れ値の影響を排除
- **STATE_ZERO遷移デバウンス**: 連続3サンプル（300ms）ゼロ判定でZERO遷移。触れによる一瞬の値落ちで誤遷移してハードウェア競合が起きる不具合を防止
- **ZERO遷移時の音声通知**: 行わない（画面表示"ZERO"のみで通知。DAC/I2S競合による不具合を根本的に排除）
- **HTTPリダイレクト追跡**: 302リダイレクトの自動処理

## 5. 運用仕様

### 5.1 製品切替手順
1. M5Stack電源OFF
2. SDカードを取り出し
3. PCでconfig.json編集
4. 対応音声ファイル配置
5. SDカードを戻しM5Stack起動

### 5.2 設定例

#### 5.2.1 柿選果設定
- 規格：6L, 5L, 4L, 3L, 2L, L, M, S, 2S, 3S
- 重量範囲：50g〜999g（10段階）
- 色分け：赤→紫→青→緑→黄→オレンジ→グレー

#### 5.2.2 りんご選果設定
- 規格：特A, A, B
- 重量範囲：300-400g, 200-299g, 100-199g
- 色分け：赤、緑、青

### 5.3 メンテナンス
- 定期キャリブレーション（100g標準重量使用）
- SDカード容量確認
- 音声ファイル整合性確認
- WiFi接続状態確認
- GAS URL有効性確認
- ネットワーク通信状況確認

## 6. 制約事項

### 6.1 技術的制約
- config.jsonは必須（デフォルト設定なし）
- SDカード必須（本体ストレージ不使用）
- WiFi環境必須（オンラインモード使用時）
- 音声ファイルはWAV形式のみ

### 6.2 運用制約
- 設定変更時はシステム再起動必須
- 同時測定不可（1個ずつ測定）
- 環境温度による重量ドリフト可能性

## 7. 拡張性

### 7.1 対応可能な追加機能
- 規格数の増減（config.json編集）
- 新製品対応（設定ファイル変更）
- 音声言語変更（音声ファイル差し替え）
- 表示色カスタマイズ（16進カラー指定）

### 7.2 システム連携
- Google Apps Script経由でのデータ収集
- HTTPS通信によるセキュア通信
- HTTPリダイレクト対応による高い互換性
- 外部システムへのAPI連携拡張可能
- 複数台M5Stackの同時運用対応